# Automation-Project

This repository contains a Java-based automation framework built using the **Maven** build tool. The project follows the **Page Object Model (POM)** design pattern to ensure code maintainability and reusability.

## 🚀 Project Structure

* **src/**: Contains the main automation scripts and page objects.
* **target/**: Compiled files and build artifacts.
* **test-output/**: Reports generated after running test suites.
* **pom.xml**: Manages dependencies, plugins, and project configuration.

## 🛠️ Technologies Used
* **Java**: Core programming language.
* **Maven**: Dependency management and build lifecycle.
* **Selenium/TestNG**: (Assumed) For web automation and test execution.

## ⚙️ Setup & Installation
1. Clone the repository.
2. Ensure you have Maven installed.
3. Run `mvn clean install` to download dependencies and build the project.
